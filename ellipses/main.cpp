#include "ellipse.h"
using namespace std;

int main() 
{
	ellipse eps0;
	ellipse eps1({3, 0, 0});
	vector<double> sol;
	
	sol = eps0.cross(eps1);	//
	cout << y[0] << " " << y[1] << endl;

	
	return 0;
}
